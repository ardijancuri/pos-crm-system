-- Create the database
CREATE DATABASE pos_crm;

-- Connect to the database (run this in psql after creating the database)
\c pos_crm;

-- Grant privileges (if needed)
GRANT ALL PRIVILEGES ON DATABASE pos_crm TO postgres; 